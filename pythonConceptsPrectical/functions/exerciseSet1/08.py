# 8. **StringLength Function:**
#    Define a function named `string_length` that takes a string as input and returns its length.

def string_length(txt):
    return len(txt)

txt = input("Enter String : ")
print(string_length(txt))